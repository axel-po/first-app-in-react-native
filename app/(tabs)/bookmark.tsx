import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Bookmark = () => {
  return (
    <View>
      <Text className="text-red-600">Bookmark</Text>
    </View>
  );
};

export default Bookmark;

const styles = StyleSheet.create({});
